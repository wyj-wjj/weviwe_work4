# 企业话术智能检索与统一培训管理系统 MVP PRD

## 1. 文档目的

本文档基于 `PRD.md` 原始方案，整理首期 MVP 的产品范围、技术策略、AI 检索链路、权限规则、版本发布机制和部署路径。

首期目标不是一次性实现完整平台，而是优先跑通一条真实可用的业务闭环：

1. 管理员维护并发布官方话术内容。
2. 系统按权限隔离通用级与全量级内容。
3. 员工在前台查看最新必读、标准话术和巩固测试。
4. 员工通过 AI 问答检索当前权限内的有效话术。
5. AI 回答必须基于已发布、未失效、可见范围内的话术来源。
6. 未命中问题被记录，供管理员后续补充话术库。
7. 本地验证通过后，系统可以平滑迁移到阿里云服务器公网访问。

## 2. 首期范围结论

### 2.1 首期采用的方案

首期采用“本地优先、云端可迁移”的 MVP 架构：

- 前端：Vue 3 + Vite。
- 后端：Python FastAPI。
- 业务数据库：MySQL，首期使用本地 MySQL。
- 向量数据库：Docker 环境下的 Milvus。
- 生成模型：阿里云 DashScope OpenAI-compatible，首期使用 `qwen-plus`。
- Embedding 模型：阿里云文本向量模型，首期使用 `text-embedding-v4`。
- AI 能力：真实 RAG 问答，不做纯关键词模拟。
- 内容导入：首期不做文件上传与 AI 自动解析，仅做后台手动录入、编辑、发布。
- 权限体系：三类独立员工账号，分别为通用权限账号、完整权限账号和管理员账号；内容权限分为通用级与全量级。
- 测验结果：不落库，不记录分数，不做统计。
- 部署路径：先本地开发验证，后续迁移到阿里云服务器公网访问。

### 2.2 首期不做的内容

以下内容不纳入 MVP 首期开发：

- Word/PDF/PPT 文件上传解析。
- AI 自动结构化抽取话术草稿。
- 多岗位细分标签权限，例如销售、投融资、高管分别授权。
- 定时发布。
- 系统内消息推送。
- 数据统计看板。
- 在线审批流。
- 多人协作编辑。
- 回答初稿审批流。
- 强制考核、排行、测验分数留存。
- 完整 Docker Compose 生产部署编排。

这些能力可以作为二期扩展，不影响首期核心数据模型和接口边界。

## 3. 用户角色与权限

系统保留三类账号。公司每个人都使用自己的独立账号登录，每个账号只对应一个员工。账号类型决定后台权限和内容可见范围，内容权限按通用级与全量级两级处理。

| 账号类型 | 适用人群 | 内容可见范围 | 后台权限 | 登录方式 |
| --- | --- | --- | --- | --- |
| 管理员账号 | 内容负责人 | 通用级 + 全量级 | 全部后台管理权限 | 独立账号密码 |
| 完整权限账号 | 销售、投融资岗、高管 | 通用级 + 全量级 | 无后台权限 | 每人独立账号密码 |
| 通用权限账号 | 普通同事、一般对接人员 | 仅通用级 | 无后台权限 | 每人独立账号密码 |

权限规则：

- 前台列表、详情、AI 检索、历史归档、测验题目都必须按当前账号权限过滤。
- 通用权限账号不能检索、查看、引用全量级内容。
- 完整权限账号可以查看通用级和全量级内容，但不能进入后台。
- 管理员账号可进入后台，也可在前台查看全部内容。
- 所有账号均为个人独立账号，便于权限管理、未命中问题追踪和后续审计。

## 4. 前台用户端需求

### 4.1 登录

用户通过账号密码登录。

登录成功后，后端返回用户身份、账号类型、内容权限级别和访问令牌。前端基于身份展示对应页面入口；后端仍负责最终权限校验。

移动端需优先适配微信内置浏览器，PC 浏览器保持可用。

### 4.2 首页

首页作为默认落地页，仅保留三个核心入口：

1. 最新必读。
2. 标准话术。
3. 巩固测试。

页面顶部提供 AI 搜索入口，用户可直接输入自然语言问题。

### 4.3 最新必读

用于展示已发布的增量口径更新。

功能要求：

- 按发布时间倒序展示。
- 列表显示更新标题、发布时间、生效时间、权限级别。
- 点击后查看完整更新内容和调整要点。
- 仅展示当前账号可见范围内的内容。
- 通用权限账号只看到通用级更新；完整权限账号和管理员看到通用级与全量级更新。

### 4.4 标准话术

标准话术分为两个区域。

核心基础话术：

- 分为通用版和全量版。
- 默认展示 3-5 条精简要点。
- 支持查看完整话术。
- 支持一键复制完整话术文本。
- 根据当前账号权限展示对应版本。

标准化话术条目：

- 使用固定结构：场景、推荐说法、禁用说法、注意事项、更新时间。
- 支持按场景分类筛选。
- 支持查看详情。
- 支持复制推荐说法或完整条目。
- 仅展示当前账号可见范围内的已发布内容。

### 4.5 巩固测试

巩固测试只用于员工自查，不做考核。

功能要求：

- 每次展示 5-10 道题。
- 题目由管理员在后台手动维护。
- 题目按权限分为通用级和全量级。
- 用户提交后当场显示对错、正确答案、解析和关联话术入口。
- 不保存答题记录。
- 不保存分数。
- 不做个人统计或管理统计。

### 4.6 AI 智能问答

用户可在前台通过自然语言提问。

回答流程：

1. 后端识别当前用户权限。
2. 使用阿里云文本向量模型生成问题向量。
3. 在 Milvus 中按权限、发布状态、生效状态进行过滤后检索候选内容。
4. 后端从 MySQL 读取候选内容的权威正文。
5. 若候选内容不满足命中阈值，返回未命中提示并记录问题。
6. 若命中有效内容，将来源正文传入阿里云大模型生成回答。
7. 前端展示回答正文、引用来源、更新时间和复制按钮。

回答边界：

- 回答事实、结论、数据必须来自命中的话术原文。
- 允许调整语序、整理结构、改写为更顺畅的表达。
- 不允许新增原文未包含的信息。
- 不允许基于模型常识补充业务结论。
- 不允许低权限账号通过问答获得高权限内容。

未命中提示：

> 当前没有有效标准口径，请联系管理员。

未命中问题需要记录问题文本、提问账号、账号权限、提问时间。

## 5. 后台管理端需求

后台仅管理员可访问。

### 5.1 内容维护

后台提供以下内容维护能力：

- 核心基础话术维护。
- 标准化话术条目维护。
- 最新必读维护。
- 历史版本查看。
- 巩固测试题维护。
- 员工账号管理。
- 账号权限级别配置。
- 未命中问题查看。

### 5.2 内容状态

内容至少包含以下状态：

- 草稿：可编辑，不对前台展示，不进入 Milvus 检索。
- 已发布：前台可见，进入 Milvus 检索。
- 已下线：前台当前内容不可见，不参与 AI 检索，可进入历史归档。

### 5.3 发布与版本

发布规则采用“发布即生成版本”：

- 管理员可反复编辑草稿。
- 点击发布后立即生效。
- 每次发布生成新的版本号。
- 同一内容再次发布时，旧版本自动进入历史归档。
- 旧版本不参与当前前台展示。
- 旧版本不参与 AI 检索。
- 发布时间即首期的生效时间。

### 5.4 Milvus 索引同步

MySQL 是唯一权威数据源，Milvus 只保存检索索引。

同步规则：

- 内容发布成功后，后端生成 embedding 并写入 Milvus。
- 内容修改并重新发布后，旧向量失效，新版本重新写入。
- 内容下线后，对应向量从有效检索范围移除。
- Milvus 元数据至少包含内容 ID、版本 ID、内容类型、权限级别、发布状态、生效时间、失效时间。
- AI 回答前必须回查 MySQL 正文，不能只依赖 Milvus 里的片段内容作为权威数据。

### 5.5 账号管理

管理员可管理通用权限账号和完整权限账号：

- 新增账号。
- 编辑账号名称、员工展示名、权限级别和状态。
- 重置密码。
- 禁用账号。

密码必须加密存储。

### 5.6 未命中问题管理

后台提供未命中问题列表：

- 问题文本。
- 提问时间。
- 提问账号。
- 账号权限。
- 处理状态。

首期只提供查看和标记处理，不做统计看板。

## 6. RAG 检索与生成策略

首期 RAG 不引入 LangGraph。MVP 的问答链路是确定性的服务编排：鉴权、检索、回查正文、组装上下文、生成回答、返回来源。该流程用 FastAPI service 分层即可清晰实现。LangGraph 更适合长流程、状态机、多 Agent、人机协同、可恢复工作流等复杂场景，首期引入会增加架构复杂度。后续如果出现多步骤任务编排、人工介入审核、长时会话工作流，再评估引入。

### 6.1 数据流

内容发布链路：

1. 管理员编辑内容草稿。
2. 管理员点击发布。
3. 后端写入 MySQL 当前版本和历史版本记录。
4. 后端调用阿里云 embedding 模型生成向量。
5. 后端写入或更新 Milvus 索引。
6. 前台和 AI 问答立即使用新内容。

问答链路：

1. 用户提交问题。
2. 后端校验登录状态和权限。
3. 调用阿里云 embedding 模型生成问题向量。
4. Milvus 按权限和有效状态检索候选内容。
5. 后端根据阈值判断是否命中。
6. 命中后从 MySQL 读取正文和来源信息。
7. 调用阿里云生成模型输出引用式回答。
8. 返回回答、来源列表、更新时间。
9. 未命中则记录问题并返回固定提示。

### 6.2 检索策略

MVP 首期采用语义检索为主，结合结构化过滤。

必须过滤的元数据：

- 权限级别。
- 内容状态。
- 内容类型。
- 生效时间。
- 失效时间。

建议检索策略：

- 问题向量检索 topK 候选内容。
- 设置最低相似度阈值。
- 候选内容数量有限时，直接将候选正文传给生成模型。
- 若候选过多，只传入最相关的若干条，避免上下文污染。

二期可扩展混合检索：

- MySQL 全文检索或关键词检索。
- Milvus 语义检索。
- 结果融合与重排。

### 6.3 Chunk 策略

由于首期内容由后台结构化录入，不做长文档解析，chunk 以业务条目为边界：

- 核心基础话术可按完整话术和要点分别建立索引。
- 标准化话术条目以单条场景为索引单位。
- 最新必读以单条更新为索引单位。
- 每个向量条目保留父内容 ID 和版本 ID。

不采用固定字符长度切分，避免切断业务语义。

### 6.4 后端 RAG 服务分层

RAG 后端建议拆分为以下服务：

`RagIndexService`：

- 负责内容发布、重新发布、下线时的索引维护。
- 从 MySQL 读取已发布版本。
- 按业务语义生成 chunk。
- 调用阿里云文本向量模型生成 embedding。
- 写入 Milvus。
- 在 MySQL 中记录向量索引映射。

`RagSearchService`：

- 负责问答前的检索。
- 校验用户身份和内容权限。
- 调用阿里云文本向量模型生成问题向量。
- 在 Milvus 中按权限、内容状态、生效时间、失效时间进行过滤。
- 返回 topK 候选 chunk。
- 根据相似度阈值过滤弱相关结果。

`RagAnswerService`：

- 负责上下文管理和回答生成。
- 根据 Milvus 返回的 `content_id`、`version_id`、`chunk_id` 回查 MySQL 正文。
- 只将最相关的若干条内容组装为模型上下文。
- 调用阿里云生成模型输出引用式回答。
- 返回回答正文、来源列表、更新时间和复制所需文本。

`MissedQuestionService`：

- 负责记录未命中问题。
- 当检索为空或低于阈值时，返回固定未命中提示。
- 不让模型基于自身知识自由回答。

### 6.5 上下文管理

上下文管理遵循以下规则：

- RAG 上下文只能来自当前账号可见、已发布、未失效的内容。
- Milvus 只返回候选索引，进入 prompt 的正文必须从 MySQL 回查。
- 不把所有检索结果塞入 prompt，只取最相关的 3-5 条。
- 若多个 chunk 来自同一内容版本，应合并去重，避免重复上下文污染回答。
- 若存在多个版本，只使用当前有效版本。
- 历史版本可用于后台追溯，但不进入当前问答上下文。
- 对话历史最多用于理解追问指代，不能作为事实来源。
- 模型提示词必须明确：资料不足时返回“当前没有有效标准口径，请联系管理员”。
- 返回给前端的来源必须与实际进入 prompt 的上下文一致。

上下文组装建议包含：

- 用户问题。
- 当前用户权限级别。
- 检索到的来源片段。
- 每个片段的标题、内容类型、更新时间、版本号。
- 严格回答边界说明。

### 6.6 对话存储

对话记录可以放在 MySQL 中，便于后续审计、问题复盘和多轮追问。

MVP 有两种实现层级：

- 基础层级：只做单轮问答，不保存完整对话，仅保存未命中问题。
- 增强层级：保存会话线程、用户问题、AI 回答和引用来源。

若实现多轮问答，建议将以下数据写入 MySQL：

- 会话线程。
- 用户消息。
- AI 回答。
- 本次回答使用的来源内容。

对话历史的使用边界：

- 对话历史可以帮助理解“这个、刚才那个、上一条”这类追问。
- 对话历史不能替代话术库作为事实来源。
- 每次回答仍必须重新检索当前有效话术。
- 如果历史对话中包含已下线或用户当前不可见内容，不能继续作为本轮回答依据。

### 6.7 MySQL 与 Milvus 的对应关系

MySQL 与 Milvus 不是简单的“一条内容记录对应一条向量记录”，而是权威数据与检索索引的关系。

推荐关系如下：

```text
MySQL contents 1 条
  -> MySQL content_versions N 条
    -> MySQL content_chunks N 条
      -> Milvus vectors N 条
```

MySQL 是唯一权威数据源：

- 保存账号、权限、内容正文、版本、chunk、测验题、未命中问题和对话记录。
- 判断内容是否已发布、是否失效、当前用户是否可见。
- 提供 AI 回答时的权威正文。

Milvus 是检索索引：

- 保存向量和必要过滤元数据。
- 每条向量必须能回指到 MySQL 的 `content_id`、`version_id`、`chunk_id`。
- 草稿内容不进入 Milvus。
- 下线内容和历史版本不进入当前有效检索范围。
- 内容重新发布后应生成新的 chunk 和向量索引，旧索引标记失效或删除。

这种关系保证：检索依赖 Milvus 的语义召回能力，但回答、权限和版本有效性最终以 MySQL 为准。

### 6.8 阿里云模型与调用配置

系统通过阿里云 DashScope 接入模型服务。API Key 必须从环境变量读取，禁止在代码、前端包、文档示例或数据库中硬编码。

首期必用配置：

| 用途 | 配置项 | 值 |
| --- | --- | --- |
| API Key | `DASHSCOPE_API_KEY` | 从环境变量读取 |
| OpenAI-compatible base URL | `DASHSCOPE_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| 生成模型 | `DASHSCOPE_CHAT_MODEL` | `qwen-plus` |
| 文本向量接口 | `DASHSCOPE_EMBEDDING_URL` | `https://dashscope.aliyuncs.com/api/v1/services/embeddings/text-embedding/text-embedding` |
| 文本向量模型 | `DASHSCOPE_EMBEDDING_MODEL` | `text-embedding-v4` |
| 生成温度 | `DASHSCOPE_TEMPERATURE` | `0.2` |

首期调用边界：

- `qwen-plus` 只用于基于检索上下文生成引用式回答。
- `text-embedding-v4` 用于内容发布时生成 chunk 向量，以及用户提问时生成 query 向量。
- `temperature` 默认设为 `0.2`，优先保证回答稳定、保守、可复现。
- 后端统一封装 DashScope 调用，前端不得直接调用阿里云模型接口。
- 生成模型调用必须带入系统提示词，明确禁止输出来源材料之外的事实、数据、结论或推断。
- Embedding 调用失败时，不影响内容在前台展示，但该内容暂不能进入 AI 检索命中范围。

二期预留配置：

| 用途 | 配置项 | 值 | 用途说明 |
| --- | --- | --- | --- |
| 视觉模型 | `DASHSCOPE_VISION_MODEL` | `qwen3.5-flash` | 预留给图片/截图理解，不纳入 MVP |
| OCR 模型 | `DASHSCOPE_OCR_MODEL` | `qwen-vl-ocr-2025-11-20` | 预留给 PDF/PPT/图片型文档解析，不纳入 MVP |
| 多模态向量接口 | `DASHSCOPE_MULTIMODAL_EMBEDDING_URL` | `https://dashscope.aliyuncs.com/api/v1/services/embeddings/multimodal-embedding/multimodal-embedding` | 预留给图文混合检索，不纳入 MVP |
| 多模态向量模型 | `DASHSCOPE_MULTIMODAL_EMBEDDING_MODEL` | `qwen3-vl-embedding` | 预留给图文混合检索，不纳入 MVP |

MVP 不依赖视觉、OCR 或多模态 embedding。相关配置可以放入 `.env.example` 作为预留项，但实现阶段不需要开发对应业务流程。

## 7. 数据模型建议

以下为 MVP 数据模型方向，具体字段可在实现阶段细化。

### 7.1 用户与权限

`users`：

- id
- username
- password_hash
- display_name
- account_type：admin、full_user、general_user
- content_level：general、full
- is_active
- created_at
- updated_at

### 7.2 内容主体

`contents`：

- id
- content_type：base_script、script_item、must_read
- title
- category
- permission_level：general、full
- status：draft、published、offline
- current_version_id
- created_by
- created_at
- updated_at

`content_versions`：

- id
- content_id
- version_no
- title
- summary
- body
- structured_payload
- published_at
- effective_at
- expired_at
- created_by
- created_at

`content_chunks`：

- id
- content_id
- version_id
- chunk_type：summary、body、scenario、qa、update
- chunk_text
- sort_order
- token_estimate
- content_hash
- permission_level
- is_active
- created_at

`vector_index_records`：

- id
- content_id
- version_id
- chunk_id
- milvus_collection
- milvus_primary_key
- embedding_model
- embedding_dimension
- content_hash
- indexed_at
- is_active

### 7.3 测验

`quiz_questions`：

- id
- question
- options
- answer
- explanation
- related_content_id
- permission_level
- status
- created_at
- updated_at

不需要答题记录表。

### 7.4 未命中问题

`missed_questions`：

- id
- question
- user_id
- account_type
- content_level
- asked_at
- status：new、handled
- handled_at

### 7.5 对话记录

`conversation_threads`：

- id
- user_id
- title
- created_at
- updated_at

`conversation_messages`：

- id
- thread_id
- user_id
- role：user、assistant
- content
- created_at

`rag_answer_sources`：

- id
- message_id
- content_id
- version_id
- chunk_id
- source_title
- source_updated_at
- relevance_score

## 8. 前后端工程策略

### 8.1 后端

后端采用 Python FastAPI。

模块建议：

- auth：登录、鉴权、密码加密、令牌校验。
- users：账号管理。
- contents：内容维护、发布、下线、版本归档。
- quiz：测验题管理和前台取题。
- rag：embedding、Milvus 检索、阿里云生成模型调用、回答约束。
- missed_questions：未命中问题记录与后台查看。
- conversations：对话线程、消息和回答来源记录。
- config：环境变量和外部服务配置。

后端通过环境变量管理：

- MySQL 连接信息。
- Milvus 连接信息。
- `DASHSCOPE_API_KEY`。
- `DASHSCOPE_BASE_URL`。
- `DASHSCOPE_CHAT_MODEL`。
- `DASHSCOPE_EMBEDDING_URL`。
- `DASHSCOPE_EMBEDDING_MODEL`。
- `DASHSCOPE_TEMPERATURE`。
- 二期预留的视觉、OCR 和多模态 embedding 配置。
- JWT 密钥。
- 前端允许访问的域名。

### 8.2 前端

前端采用 Vue 3 + Vite。

页面建议：

前台：

- 登录页。
- 首页。
- 最新必读列表与详情。
- 标准话术列表与详情。
- 巩固测试页。
- AI 问答结果页或问答弹层。

后台：

- 后台登录后首页。
- 内容列表。
- 内容编辑页。
- 发布确认。
- 历史版本页。
- 测验题管理。
- 账号管理。
- 未命中问题列表。

组件策略：

- 移动 H5 优先保证前台体验。
- 后台管理端优先 PC 操作效率。
- 可在同一 Vue 工程中用路由区分前台与后台。
- API 调用统一封装，处理登录失效、权限不足和服务异常。

## 9. 错误处理与边界规则

### 9.1 权限不足

当用户访问无权限内容时：

- 后端返回权限不足错误。
- 前端提示“无权查看该内容”。
- 不暴露内容标题、正文、来源等敏感信息。

### 9.2 AI 未命中

当检索无有效结果或低于阈值：

- 不调用生成模型自由回答，或不采信模型回答。
- 返回固定提示。
- 写入未命中问题表。

### 9.3 外部服务异常

当阿里云模型或 Milvus 不可用：

- 前端提示“智能问答暂不可用，请稍后重试”。
- 不影响手动查看标准话术。
- 后端记录错误日志。

### 9.4 向量同步失败

当内容发布成功但向量同步失败：

- MySQL 仍保留已发布内容。
- 前台标准话术正常可见。
- AI 检索暂时无法命中该内容。
- 后台应能看到索引状态，管理员可重试同步。

## 10. 本地验证与后续上云

### 10.1 本地验证

本地阶段目标是验证功能闭环。

运行形态：

- 本机 MySQL。
- Docker Milvus。
- FastAPI 后端本地启动。
- Vue 3 + Vite 前端本地启动。
- `.env` 管理全部外部连接配置。

本地验收重点：

- 登录与权限隔离正确。
- 管理员可维护并发布内容。
- 发布后前台立即可见。
- 发布后 Milvus 可检索。
- AI 回答能显示来源。
- 低权限账号无法看到或检索全量级内容。
- 未命中问题能被记录。
- 测验不落库。

### 10.2 阿里云部署方向

本地验证通过后，部署到阿里云服务器公网访问。

推荐部署形态：

- 阿里云 ECS 承载后端、前端静态资源和 Milvus。
- MySQL 可先部署在 ECS 或使用阿里云 RDS。
- Nginx 负责前端静态资源、API 反向代理、HTTPS。
- 使用域名和 SSL 证书提供公网访问。
- 后端通过进程守护或容器运行。
- `.env` 或服务器环境变量管理生产配置。

上线前需要补充：

- 域名与 HTTPS。
- 数据库备份策略。
- 日志留存策略。
- 管理员初始账号创建方式。
- 阿里云 API Key 安全配置。
- 防火墙与安全组规则。

## 11. MVP 验收标准

MVP 可验收的标准如下：

1. 管理员可以登录后台。
2. 管理员可以创建通用级和全量级内容草稿。
3. 管理员可以发布内容，发布后生成版本。
4. 已发布内容能在前台按权限展示。
5. 已发布内容能同步进入 Milvus 检索范围。
6. 通用权限账号不能查看或检索全量级内容。
7. 完整权限账号可以查看和检索通用级与全量级内容。
8. AI 问答能基于命中内容生成回答。
9. AI 问答结果展示来源和更新时间。
10. 未命中问题返回固定提示并写入后台列表。
11. 测验提交后只在当前页面展示对错和解析，不写入答题记录。
12. 历史版本可查看，但不参与当前检索。
13. 本地环境可以完整跑通前端、后端、MySQL、Milvus 和阿里云模型调用。

## 12. 后续扩展方向

二期可在 MVP 基础上增加：

- Word/PDF/PPT 上传。
- AI 结构化解析草稿。
- OCR、视觉模型和多模态 embedding，用于图文混合材料解析与检索。
- 内容敏感级别自动建议。
- 多岗位标签权限。
- 定时发布。
- 混合检索和重排。
- Docker Compose 一键部署。
- 阿里云 RDS、对象存储、日志服务等托管能力。
- 更完整的运维监控和备份恢复方案。
